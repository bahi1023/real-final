apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "motivational-app.fullname" . }}-test-connection"
  labels:
    {{- include "motivational-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "motivational-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
